import React from 'react';
import Section from './primitives/Section';

const SkillsSection = ({heading, children}) => {
    return <Section heading="Skills">

    </Section>
}

export default SkillsSection;