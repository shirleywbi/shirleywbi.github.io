import React from 'react';
import Section from './primitives/Section';

const ProjectSection = ({heading, children}) => {
    return <Section heading="Projects">

    </Section>
}

export default ProjectSection;